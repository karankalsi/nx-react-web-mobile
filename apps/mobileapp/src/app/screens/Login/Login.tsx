import React from 'react';
import { View } from 'react-native';
import LoginStyles from './Login.styles';

const Login: React.FC = () => {
  return <View style={LoginStyles.container}></View>;
};

export default Login;
